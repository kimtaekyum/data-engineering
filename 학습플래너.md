# 데이터 엔지니어링 5일 학습 체크리스트

기준 파일: `코드 핵심정리.md`  
목표: 5일 안에 시험에서 풀 수 있는 수준으로 핵심 코드, 개념 차이, 서술형 설명을 준비한다.  
학습 방식: 완벽한 정독보다 `대표 코드 이해 -> 결과 예측 -> 말로 설명` 순서로 압축한다.

## 범위 매핑

| 시험 장 | 실습 파일 | 핵심 주제 |
|---|---|---|
| 2장 | `01_numpy_arrays.ipynb` | NumPy 배열, 인덱싱, 슬라이싱, view/copy |
| 3장 | `02_numpy_ufunc.ipynb`, `03_numpy_advanced.ipynb` | UFunc, 브로드캐스팅, 마스크, 팬시 인덱싱 |
| 4장 | `04_pandas_objects.ipynb`, `05_data_manipulation.ipynb` | Series, DataFrame, `loc`, `iloc` |
| 5장 | `06_combining_datasets.ipynb`, `07_eda_inst.ipynb` | `concat`, `merge`, EDA |
| 6장 | `08_web_scraping.ipynb`, `09_restful_api_inst.ipynb` | 웹스크래핑, REST API, JSON |
| 7장 | `10_working_with_time_series.ipynb`, `11_timeseries_specific_operations.ipynb` | datetime, resampling, shifting, rolling |
| 8장 | `12_handling_missing_data.ipynb`, `13_missing_data_imputation.ipynb` | 결측치 처리, 대치 |
| 9장 | `14_outlier_detection.ipynb` | IQR, Z-score |
| 10장 | `15_feature_extraction.ipynb` | binning, log transform, one-hot encoding, normalization |

## 사용 규칙

- [ ] 하루 시작 전에 전날 내용 20분 복습하기
- [ ] 각 장에서 대표 코드 5개 이상 직접 읽고 결과 예측하기
- [ ] 각 장 끝나면 “이 코드는 왜 쓰는가?”를 3문장으로 말해보기
- [ ] 모르는 코드는 `코드 핵심정리.md`에서 다시 찾기
- [ ] 하루 마무리 때 개념 차이 3개 이상 말로 설명하기

## Day 1

주제: `2장 + 3장`  
핵심: NumPy 배열, UFunc, 집계, 브로드캐스팅

- [ ] `np.array()` 의미 이해하기
- [ ] Python list와 NumPy array 차이 설명하기
- [ ] 배열 인덱싱, 슬라이싱 결과 예측하기
- [ ] 2차원 배열에서 `[행, 열]` 접근 익히기
- [ ] 불리언 마스크 `a[a >= 10]` 이해하기
- [ ] `ndim`, `shape`, `size`, `dtype` 구분하기
- [ ] view와 copy 차이 설명하기
- [ ] `reshape()` 가능 조건 말하기
- [ ] `np.concatenate`, `vstack`, `hstack`, `split` 구분하기
- [ ] `np.add`, `np.subtract`, `np.multiply`, `np.power` 확인하기
- [ ] `np.linspace()`와 `np.arange()` 차이 설명하기
- [ ] `reduce()`와 `accumulate()` 차이 설명하기
- [ ] `sum`, `mean`, `std`, `argmax`, `argmin` 용도 정리하기
- [ ] `axis=0`과 `axis=1` 결과 방향 구분하기
- [ ] 브로드캐스팅 예제 2개 직접 눈으로 풀어보기
- [ ] 팬시 인덱싱 기본 개념 이해하기

Day 1 마무리 점검

- [ ] `view vs copy`를 말로 설명할 수 있다
- [ ] `arange vs linspace`를 말로 설명할 수 있다
- [ ] `axis=0 vs axis=1`을 말로 설명할 수 있다
- [ ] NumPy 대표 코드 10개를 보고 의미를 바로 말할 수 있다

## Day 2

주제: `4장 + 5장`  
핵심: Pandas 객체, 선택, 결합, EDA

- [ ] `pd.Series()`와 `pd.DataFrame()` 차이 이해하기
- [ ] 명시적 인덱스와 위치 인덱스 차이 정리하기
- [ ] `df.index`, `df.columns`, `df.values` 의미 구분하기
- [ ] 한 컬럼 선택과 여러 컬럼 선택 결과 차이 확인하기
- [ ] `loc`와 `iloc` 차이 설명하기
- [ ] Pandas 마스킹과 조건 필터링 복습하기
- [ ] 파생 컬럼 생성 흐름 이해하기
- [ ] `pd.concat()` 사용 상황 정리하기
- [ ] `pd.merge()` 사용 상황 정리하기
- [ ] inner join, left join, outer join 차이 정리하기
- [ ] join 후 생기는 `NaN` 의미 생각해보기
- [ ] `df.info()`와 `df.describe()` 역할 구분하기
- [ ] EDA가 왜 필요한지 3문장으로 정리하기
- [ ] 분포 확인, 결측치 확인, 상관관계 확인 흐름 이해하기

Day 2 마무리 점검

- [ ] `loc vs iloc`를 예시와 함께 설명할 수 있다
- [ ] `concat vs merge`를 말로 설명할 수 있다
- [ ] `inner vs left vs outer join`을 말로 설명할 수 있다
- [ ] EDA의 목적을 데이터 엔지니어링 관점으로 설명할 수 있다

## Day 3

주제: `6장 + 7장`  
핵심: 데이터 수집, API, 시계열 처리

- [ ] `requests.get()` 흐름 이해하기
- [ ] User-Agent를 왜 넣는지 설명하기
- [ ] `BeautifulSoup(res.text, 'html.parser')` 의미 이해하기
- [ ] `select()`로 태그 찾는 흐름 익히기
- [ ] 텍스트 추출과 링크 추출 차이 이해하기
- [ ] 스크래핑 결과를 DataFrame으로 바꾸는 흐름 익히기
- [ ] API 요청 URL, 파라미터, JSON 응답 구조 이해하기
- [ ] rate limit 의미 정리하기
- [ ] 웹스크래핑과 API 차이 비교하기
- [ ] 문자열 날짜와 datetime 차이 이해하기
- [ ] `pd.to_datetime()` 필요성 설명하기
- [ ] `strftime()` 역할 정리하기
- [ ] `resample()`이 시간 단위를 바꾸는 작업임을 이해하기
- [ ] `shift()`와 `rolling()` 차이 이해하기
- [ ] 이동 평균이 왜 필요한지 생각해보기

Day 3 마무리 점검

- [ ] `웹스크래핑 vs API`를 설명할 수 있다
- [ ] `문자열 날짜 vs datetime`을 설명할 수 있다
- [ ] `resampling vs rolling`을 설명할 수 있다
- [ ] 데이터 수집부터 시계열 처리까지 흐름을 말할 수 있다

## Day 4

주제: `8장 + 9장`  
핵심: 결측치 처리, 대치, 이상치 탐지

- [ ] `None`과 `NaN` 차이 이해하기
- [ ] `isnull()`, `notnull()` 사용 목적 익히기
- [ ] 결측치 제거와 결측치 대치 차이 정리하기
- [ ] forward fill, backward fill 의미 익히기
- [ ] 평균 대치와 중앙값 대치 차이 정리하기
- [ ] 시계열에서 forward fill이 왜 자주 쓰이는지 생각하기
- [ ] MAE로 대치 성능을 비교하는 이유 이해하기
- [ ] 이상치의 의미를 결측치와 구분해서 말하기
- [ ] IQR 계산 흐름 정리하기
- [ ] `Q1`, `Q3`, `IQR` 의미 정리하기
- [ ] `Q1 - 1.5 * IQR`, `Q3 + 1.5 * IQR` 기준 암기하기
- [ ] Z-score가 평균과 표준편차 기반이라는 점 정리하기
- [ ] `abs(z) > 3` 기준 의미 이해하기
- [ ] 이상치가 평균, 분산, 분석 결과에 미치는 영향 생각하기

Day 4 마무리 점검

- [ ] `결측치 vs 이상치`를 설명할 수 있다
- [ ] `결측치 제거 vs 결측치 대치`를 설명할 수 있다
- [ ] `IQR vs Z-score`를 설명할 수 있다
- [ ] 결측치와 이상치 문제를 서술형으로 답할 수 있다

## Day 5

주제: `10장 + 전체 총복습`  
핵심: 특징 변환, 개념 비교, 시험 직전 정리

- [ ] `pd.cut()` 구간화 의미 이해하기
- [ ] binning을 왜 하는지 설명하기
- [ ] `np.log1p()` 로그 변환 목적 이해하기
- [ ] `pd.get_dummies()` 원핫인코딩 의미 이해하기
- [ ] normalization과 standardization 차이 확인하기
- [ ] 수치형 데이터와 범주형 데이터 처리 방식 구분하기
- [ ] `one-hot encoding vs normalization` 설명하기
- [ ] 시험 직전 코드 암기표 전체 다시 보기
- [ ] 개념 차이 말하기 정리 전체 다시 보기
- [ ] 각 장에서 대표 코드 2개씩만 다시 체크하기
- [ ] 서술형 답안 3개 써보기
- [ ] 가장 헷갈리는 개념 5개만 따로 적기

Day 5 최종 점검

- [ ] `loc/iloc`, `concat/merge`, `view/copy`, `axis`, `IQR/Z-score`를 바로 설명할 수 있다
- [ ] 대표 코드 보고 결과를 예측할 수 있다
- [ ] 서술형 답안을 4문장 구조로 쓸 수 있다
- [ ] 새 내용 추가 없이 복습만 한다

## 시험 직전 최소 체크

시간이 없으면 이것만 본다.

- [ ] NumPy: 슬라이싱, view/copy, `reshape`, `axis`
- [ ] Pandas: `Series`, `DataFrame`, `loc`, `iloc`, 마스킹
- [ ] 결합: `concat`, `merge`, join 종류
- [ ] 수집: 웹스크래핑 vs API
- [ ] 시계열: `to_datetime`, `resample`, `rolling`
- [ ] 결측치: 제거 vs 대치, `fillna`
- [ ] 이상치: IQR, Z-score
- [ ] 특징 변환: binning, log transform, one-hot encoding, normalization

## 서술형 답안 틀

아래 구조로 짧게 말하는 연습을 한다.

```text
정의: OO은 ~이다.
원리: ~ 기준으로 처리한다.
예시: ~ 상황에서 사용한다.
의미: 데이터 엔지니어링 관점에서 ~ 때문에 중요하다.
```

## 마지막 기준

- 모든 내용을 완벽히 외우는 것보다 `대표 코드 의미 + 개념 차이 설명`이 더 중요하다
- 모르는 문제가 나와도 비슷한 개념과 비교해서 설명하면 점수를 챙길 수 있다
- 하루가 밀리면 새 진도보다 전날 핵심 5개 복습을 먼저 한다
