# 데이터 엔지니어링 시험직전 압축본

시험 범위는 PDF 기준 `2장~10장`, 실제 실습 파일 기준 `01~15` 전체다.

## 1. 시험 범위 매핑

| PDF 장 | 실습 파일 | 핵심 |
|---|---|---|
| 2장 | `01` | NumPy 배열, 인덱싱, 슬라이싱, view/copy |
| 3장 | `02`, `03` | UFunc, 벡터화, axis, 브로드캐스팅, 마스크 |
| 4장 | `04`, `05` | Series, DataFrame, 인덱스, `loc/iloc` |
| 5장 | `06`, `07` | `concat`, `merge`, join, EDA |
| 6장 | `08`, `09` | 웹스크래핑, API, JSON |
| 7장 | `10`, `11` | datetime, resampling, shifting, rolling |
| 8장 | `12`, `13` | 결측치 처리, 결측치 대치, MAE |
| 9장 | `14` | 이상치, IQR, Z-score |
| 10장 | `15` | binning, log transform, one-hot, normalization |

## 2. 장별 핵심 5줄

### 2장
- NumPy 배열은 원소 단위 연산을 지원한다.
- 슬라이싱은 원본의 view가 될 수 있다.
- 원본 변경을 피하려면 `.copy()`를 쓴다.
- `reshape`는 전체 원소 수가 같아야 가능하다.
- 마스크는 조건에 맞는 값만 선택한다.

### 3장
- UFunc는 배열 원소에 빠르게 적용되는 함수다.
- 벡터화 연산은 Python loop보다 대량 데이터 처리에 유리하다.
- `axis=0`은 열별 결과, `axis=1`은 행별 결과다.
- 브로드캐스팅은 오른쪽 차원부터 shape을 비교한다.
- `np.newaxis`는 차원을 추가한다.

### 4장
- Series는 1차원, DataFrame은 2차원 표다.
- Pandas 인덱스는 단순 위치가 아니라 라벨이다.
- `loc`는 라벨 기준이다.
- `iloc`는 정수 위치 기준이다.
- 조건 필터링은 마스크로 수행한다.

### 5장
- `concat`은 축 방향 연결이다.
- `merge`는 공통 키 기준 결합이다.
- inner는 교집합, left는 왼쪽 보존, outer는 전체 보존이다.
- join 결과의 `NaN`은 매칭 실패의 단서다.
- EDA는 분석 전 데이터 품질 점검이다.

### 6장
- 웹스크래핑은 HTML에서 데이터를 추출한다.
- API는 정해진 인터페이스로 구조화 데이터를 요청한다.
- User-Agent는 브라우저 요청처럼 보이게 하는 헤더다.
- JSON은 분석 가능한 형태로 변환해야 한다.
- rate limit은 API 호출 제한이다.

### 7장
- 문자열 날짜는 datetime으로 변환해야 시간 연산이 가능하다.
- 시계열 인덱스는 시간 기준 선택과 집계에 중요하다.
- resampling은 시간 단위를 바꿔 집계한다.
- shifting은 값을 시간 방향으로 민다.
- rolling은 이동 창 통계를 계산한다.

### 8장
- `None`과 `NaN`은 결측 표현이다.
- `isnull()`로 결측 여부를 확인한다.
- 결측치는 제거하거나 대치한다.
- forward fill은 이전 값으로 채운다.
- MAE로 대치 성능을 비교할 수 있다.

### 9장
- 이상치는 정상 패턴에서 크게 벗어난 값이다.
- IQR은 `Q3 - Q1`이다.
- IQR 기준은 `Q1 - 1.5*IQR`, `Q3 + 1.5*IQR`이다.
- Z-score는 평균에서 표준편차 몇 배 떨어졌는지 본다.
- 이상치는 평균과 모델 성능을 왜곡할 수 있다.

### 10장
- 특징 변환은 분석/모델 입력에 맞게 데이터를 바꾸는 과정이다.
- binning은 연속형 값을 구간으로 나눈다.
- log transform은 치우친 분포를 완화한다.
- one-hot encoding은 범주형을 숫자 컬럼으로 바꾼다.
- normalization은 수치형 변수의 스케일을 맞춘다.

## 3. 대표 코드 30개

```python
np.array([1, 2, 3])
arr[2:8:2]
arr[arr >= 10]
arr[:2, :2].copy()
x.reshape(2, 5)
np.concatenate([a, b])
np.vstack([a, b])
np.hstack([a, b])
np.split(arr, [3, 5])
np.add(x, 5)
np.power(x, 2)
np.add.accumulate(x)
np.sum(arr, axis=0)
np.sum(arr, axis=1)
np.linspace(0, 100, 5)
a[:, np.newaxis]
image[rows[:, np.newaxis], cols]
pd.Series([1, 2, 3], index=['a', 'b', 'c'])
df.loc[0:3, 'col']
df.iloc[0:3, 1:4]
df[df['col'] >= 2]
pd.concat([df1, df2])
pd.merge(left, right, on='ID', how='left')
df.info()
df.describe()
requests.get(url, headers=headers)
BeautifulSoup(res.text, 'html.parser')
pd.to_datetime(df['date'])
df.isnull().sum()
pd.get_dummies(df['category'])
```

## 4. 개념 비교표

| 비교 | 핵심 |
|---|---|
| list vs ndarray | ndarray는 벡터화 연산 가능 |
| view vs copy | view는 원본 공유, copy는 별도 복사 |
| arange vs linspace | arange는 간격, linspace는 개수 |
| axis=0 vs axis=1 | axis=0은 열별, axis=1은 행별 |
| loc vs iloc | loc는 라벨, iloc는 위치 |
| concat vs merge | concat은 축 연결, merge는 키 결합 |
| inner vs left vs outer | 교집합, 왼쪽 보존, 전체 보존 |
| 웹스크래핑 vs API | HTML 추출, 구조화 데이터 요청 |
| resampling vs rolling | 시간 단위 변경, 이동 창 계산 |
| 결측치 vs 이상치 | 값 없음, 비정상 값 |
| IQR vs Z-score | 사분위수 기반, 평균/표준편차 기반 |
| one-hot vs normalization | 범주형 변환, 수치형 스케일 조정 |

## 5. 서술형 템플릿

```text
정의: OO은 ~하는 기능이다.
원리: 작동 원리는 ~ 기준으로 처리하는 것이다.
예시: 예를 들어 ~ 상황에서 사용할 수 있다.
데이터 엔지니어링 관점: 그래서 ~ 때문에 중요하다.
```

예시:

```text
Left join은 왼쪽 테이블의 모든 행을 유지하면서 오른쪽 테이블의 매칭 값을 붙이는 결합 방식이다.
오른쪽 테이블에 매칭되는 키가 없으면 해당 값은 NaN이 된다.
예를 들어 회원 테이블과 시청 로그를 결합하면 시청 기록이 없는 회원도 결과에 남는다.
데이터 엔지니어링 관점에서 이 NaN은 미활동 사용자나 누락 로그를 발견하는 단서가 될 수 있다.
```

## 6. 시험 직전 체크리스트

- [ ] 실습 `01~15` 전체가 시험 범위임을 안다.
- [ ] view/copy 차이를 설명할 수 있다.
- [ ] `axis=0`, `axis=1` 결과를 예측할 수 있다.
- [ ] 브로드캐스팅 가능 여부를 shape으로 판단할 수 있다.
- [ ] `loc/iloc` 차이를 설명할 수 있다.
- [ ] `concat/merge/join` 차이를 설명할 수 있다.
- [ ] 웹스크래핑과 API를 비교할 수 있다.
- [ ] resampling, shifting, rolling을 구분할 수 있다.
- [ ] 결측치 제거와 대치의 장단점을 말할 수 있다.
- [ ] IQR, Z-score 이상치 기준을 말할 수 있다.
- [ ] one-hot encoding과 normalization 차이를 말할 수 있다.
